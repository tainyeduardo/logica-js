// Escreva um algoritmo para ler as dimensões de um retângulo
//  (base e altura), calcular e escrever a área do retângulo.
//   Dica a=b.h

var base = parseInt(prompt("para calcular a área do retângulo, primeiro digite a base dele: "));
var altura = parseInt(prompt("agora digite a altura: "));
var area = base*altura;

alert("a area do retangulo é: "+area);
